(()=>{const tabs=[...document.querySelectorAll('[data-recorder-mode]')];function select(tab){tabs.forEach(t=>{const active=t===tab;t.setAttribute('aria-selected',String(active));t.tabIndex=active?0:-1});document.querySelectorAll('[data-recorder-panel]').forEach(p=>p.hidden=p.dataset.recorderPanel!==tab.dataset.recorderMode)}tabs.forEach((tab,i)=>{tab.addEventListener('click',()=>select(tab));tab.addEventListener('keydown',e=>{if(e.key==='ArrowLeft'||e.key==='ArrowRight'){e.preventDefault();const next=tabs[(i+1)%tabs.length];select(next);next.focus()}})})})();
