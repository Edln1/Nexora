const examples={talk:{kicker:'SPACE TO THINK',title:'Start wherever<br>your mind is.',question:'I have an idea. I’m not sure where to start.',answer:'Tell me the part you’re most excited about.<br>We can find the starting point together.'},remember:{kicker:'A THREAD TO PICK UP',title:'You don’t have to<br>start over.',question:'Can we come back to the idea from yesterday?',answer:'The little studio you wanted to open?<br>We left off at what you’d make first.'},reflect:{kicker:'ROOM TO REFLECT',title:'Make a little space<br>for yourself.',question:'It’s been a lot today.',answer:'Want to talk it through, or just slow down for a moment?<br>We don’t have to solve everything right now.'}};
document.querySelectorAll('[data-demo]').forEach(button=>button.addEventListener('click',()=>{const e=examples[button.dataset.demo];document.querySelectorAll('[data-demo]').forEach(b=>{b.classList.toggle('active',b===button);b.setAttribute('aria-pressed',String(b===button));});document.getElementById('demo-kicker').textContent=e.kicker;document.getElementById('demo-title').innerHTML=e.title;document.getElementById('demo-question').textContent=e.question;document.getElementById('demo-answer').innerHTML=e.answer;}));
const moments=[['Today','A walk, a new idea, and a little time to think.'],['Yesterday','Picked up the studio idea and found a first step.'],['Wednesday','Made space for a conversation after a busy afternoon.']];
function memoryView(mode='day',filter='Days'){
const target=document.getElementById('memory-view');if(!target)return;
if(mode==='day')target.innerHTML=`<span class="date-label">An illustrative day with Ava</span><h2>A walk, a new idea, and a little time to think.</h2><p class="record-tags">SAMPLE EXPERIENCE · Conversation &nbsp; Notes &nbsp; Reflection</p><div class="memory-filters">${['Days','Conversations','Ideas','Reflections'].map(s=>`<button class="${s===filter?'active':''}" aria-pressed="${s===filter}" data-filter="${s}">${s}</button>`).join('')}</div><div class="memory-stats"><div>Sample days<strong>3</strong></div><div>Conversations<strong>6</strong></div><div>Ideas<strong>4</strong></div><div>Reflections<strong>3</strong></div></div><div class="heatmap"><div>Moments across a day <span>ILLUSTRATIVE DATA</span></div>${Array.from({length:7},(_,r)=>`<div class="heat-row"><span>${['Monday','Tuesday','Wednesday','Thursday','Friday','Saturday','Sunday'][r]}</span>${Array.from({length:20},(_,i)=>`<i class="${i>4&&i<16&&((i+r)%3!==0)?(i+r)%2?'on':'mid':''}"></i>`).join('')}</div>`).join('')}</div><div class="day-list"><h3>${filter==='Days'?'Your days':filter}</h3>${(filter==='Days'?moments:filter==='Conversations'?[['Today','A place to think out loud.'],['Yesterday','Back to the studio idea.']]:filter==='Ideas'?[['Today','A small studio. A first collection.'],['Yesterday','What would the first version look like?']]:[['Today','A little room to pause.'],['Yesterday','The thought I wanted to come back to.']]).map((m,i)=>`<button class="day-row" data-day="${i}"><small>${m[0]} / SAMPLE</small>${m[1]}</button>`).join('')}</div>`;
else {const content={voice:['Start with a conversation.','Talk to Ava through chat and live voice in Synapses. This is an illustrative interface, not a live connection.'],memory:['A thread worth keeping.','The memory experience is built around continuity. Explore Synapses to see how Ava carries context across conversations.'],tools:['A little help around the edges.','The companion comes first. Synapses places tools, notes, and research around that experience.']}[mode];target.innerHTML=`<div class="memory-empty"><span class="date-label">AVA / ${mode.toUpperCase()}</span><div class="ava-emblem">✳</div><h2>${content[0]}</h2><p>${content[1]}</p><a href="https://synapses.cloud/" target="_blank" rel="noopener">Explore the existing app ↗</a></div>`;}
target.querySelectorAll('[data-filter]').forEach(b=>b.addEventListener('click',()=>memoryView('day',b.dataset.filter)));target.querySelectorAll('[data-day]').forEach(b=>b.addEventListener('click',()=>{target.innerHTML=`<button class="day-row" id="back-days">← Back to your days</button><span class="date-label">ILLUSTRATIVE DAY REPORT</span><h2>${b.textContent.replace('/ SAMPLE','')}</h2><p class="memory-empty">A conversation about a new idea became a smaller, more useful question: what would the first step look like? This is a sample of the proposed experience, not a real recording.</p><a class="text-link" href="https://synapses.cloud/" target="_blank" rel="noopener">Continue in Synapses ↗</a>`;document.getElementById('back-days').addEventListener('click',()=>memoryView());}));
}
document.querySelectorAll('[data-view]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('[data-view]').forEach(x=>{x.classList.toggle('active',x===b);x.setAttribute('aria-pressed',String(x===b));});memoryView(b.dataset.view);}));memoryView();
const cases=[['A little room to think.','I have an idea. I’m not sure where to start.','Tell me the part you’re most excited about. We can find the starting point together.'],['A familiar thread.','Can we come back to the idea from yesterday?','The little studio you wanted to open? We left off at what you’d make first.'],['A moment to yourself.','It’s been a lot today.','Want to talk it through, or just slow down for a moment? We don’t have to solve everything right now.'],['Another perspective.','What if we took this in a different direction?','Let’s explore that. What would you keep, and what would you want to change?']];
function accessibleTabs(selector,onSelect){const tabs=[...document.querySelectorAll(selector)];tabs.forEach((t,i)=>{function select(){tabs.forEach(x=>{x.setAttribute('aria-selected',String(x===t));x.tabIndex=x===t?0:-1;});onSelect(t,i);}t.addEventListener('click',select);t.addEventListener('keydown',e=>{let next;if(e.key==='ArrowRight'||e.key==='ArrowDown')next=(i+1)%tabs.length;if(e.key==='ArrowLeft'||e.key==='ArrowUp')next=(i-1+tabs.length)%tabs.length;if(e.key==='Home')next=0;if(e.key==='End')next=tabs.length-1;if(next!==undefined){e.preventDefault();tabs[next].focus();tabs[next].click();}});});}
accessibleTabs('[data-case]',(t,i)=>{const c=cases[i];document.getElementById('case-title').textContent=c[0];document.getElementById('case-question').textContent=c[1];document.getElementById('case-answer').textContent=c[2];document.getElementById('case-panel').setAttribute('aria-labelledby',t.id);});
const steps=[['Keep what catches your eye.','A simple, intentional way to capture a photo or a short moment, with clear control over when the camera is active.','↗'],['The moments you choose.','The goal is direct media access in an AVA experience connected to Synapses. Reliable transfer and control must be validated on real hardware.','⇄'],['A conversation with context.','The ambition is to ask Ava about a moment you saved and return to the original capture. This integration is a proposed experience, not a released feature.','✳']];
accessibleTabs('[data-step]',(t,i)=>{document.getElementById('step-title').textContent=steps[i][0];document.getElementById('step-copy').textContent=steps[i][1];document.querySelector('.step-symbol').textContent=steps[i][2];document.getElementById('step-count').textContent=`0${i+1} / 03`;document.getElementById('step-panel').setAttribute('aria-labelledby',t.id);});
document.querySelectorAll('.nav-menu').forEach(menu=>{let timer;const trigger=menu.querySelector('button');function open(v){clearTimeout(timer);menu.classList.toggle('open',v);trigger.setAttribute('aria-expanded',String(v));}menu.addEventListener('mouseenter',()=>open(true));menu.addEventListener('mouseleave',()=>{timer=setTimeout(()=>open(false),180)});trigger.addEventListener('click',()=>open(!menu.classList.contains('open')));menu.addEventListener('focusin',()=>open(true));menu.addEventListener('focusout',e=>{if(!menu.contains(e.relatedTarget))open(false)});menu.addEventListener('keydown',e=>{if(e.key==='Escape'){open(false);trigger.focus();open(false)}});menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>open(false)));document.addEventListener('pointerdown',e=>{if(!menu.contains(e.target))open(false)});});
const track=document.querySelector('.scenario-track');if(track){const buttons=[...document.querySelectorAll('[data-slide]')];function update(){buttons.forEach(b=>b.disabled=Number(b.dataset.slide)<0?track.scrollLeft<5:track.scrollLeft>=track.scrollWidth-track.clientWidth-5)}buttons.forEach(b=>b.addEventListener('click',()=>track.scrollBy({left:Number(b.dataset.slide)*(track.querySelector('article').offsetWidth+17),behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'instant':'smooth'})));track.addEventListener('scroll',update,{passive:true});window.addEventListener('resize',update);update();}
if('IntersectionObserver' in window&&!matchMedia('(prefers-reduced-motion: reduce)').matches){document.documentElement.classList.add('motion-ready');const observer=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}})},{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));}
